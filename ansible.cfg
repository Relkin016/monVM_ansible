[defaults]
remote_user    = relkin
host_key_checking = False
[privilege_escalation]
# Автоматическое использование sudo
become         = True
become_method  = sudo
become_user    = root
become_ask_pass = False
